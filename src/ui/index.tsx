import { webpack } from "replugged";
import { classNameFactory } from "../lib/utils/classNameFactory";

export const cl = classNameFactory

export const requireCreateStickerModal = webpack.getBySource("stickerInspected]:")
